# Favicon and particle sculptures

September 18, 2026. User requested a custom favicon and redesigned Ahmad.AI, InsightLoop and CloudOps visuals before enhancing chat responses.

## Revision

- Original violet AH monogram on near-black: SVG tab icon, 16/32/48px ICO and 180px Apple icon. Next metadata conventions generate links.
- Ahmad.AI: four-point agent sigil, central core, quiet orbit, breathing and bounded pointer/sway response. Replaces the flat bot.
- InsightLoop: continuous feedback ribbon with depth, bounded sway and a decorative light wave. Replaces bars.
- CloudOps: three evidence pages connect to an octahedral grounding core. Replaces cylinder; remains planned/in development.

All destinations use original hero particles in the same canvas. Hero geometry and 7,374 desktop / 3,022 compact sampling remain. Matching SVG silhouettes provide static views. No new dependency, vertex attribute or render pass. Existing project interpolation, adaptive budgets, reduced motion and cleanup remain. No backend change or provider request.

## Validation

Production build, TypeScript and lint passed. Full browser run passed 32 checks, including shader compilation, deterministic geometry, architecture coverage under adaptive budgets, one-canvas reversible morphology, project selection, responsive/200% text, keyboard and fallbacks. Its remaining scroll test used a fixed intermediate timing sample, unreliable during concurrent software rendering; replaced that sample with immediate non-jump and eventual-distance checks. All three final control tests passed.

Reviewed all three WebGL forms at 1440×960 (full quality) and 390×960 (compact), plus the monogram and generated icon links. Captures: `review/sculpture-{agent,insightloop,cloudops}-{1440,390}.png`. Chromium software WebGL is used; native-device performance is not newly measured. Ready for subjective visual review.

Next: enhance Ahmad.AI responses. Providers, knowledge base and chat API are unchanged.
